# Local development: point the frontend to the backend API.
VITE_API_URL=http://localhost:5000

# Production: set VITE_API_URL to the deployed backend URL.
# Do not commit real secrets or private environment values.
